Added support for onboarding switch stacks as Virtual Chassis objects in the Sync Devices From Network job for Cisco IOS and Cisco IOS-XE platforms.
Added Virtual Chassis master identification via serial number matching, correctly handling stacks where the conductor is not switch 1.
Added handling of provisioned-but-absent stack slots so single-member stacks with empty provisioned slots are onboarded as standalone devices.
